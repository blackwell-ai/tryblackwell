# Supabase (server-side only; used by the /api routes via the service role)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Resend — set to email brand-audit leads to founders@tryblackwell.com
RESEND_API_KEY=
# Optional verified sender; defaults to Resend's onboarding address
LEADS_FROM=
